<%- include("partials/header") %>
    <h1>Home</h1>
    <p><%= homeContent %></p>

      <% posts.forEach(function(element){ %>
        <div class="post">
          <h2> <%= element.title %> </h2>
          <p> <%= element.content.substr(0, 80) + "..." %>

          <a href="/posts/<%= element.title %>">Read More</a>
          </p>
        </div>
      <% }) %>

<%- include("partials/footer") %>
